module.exports = {
    harvesterCount : 2,
    workerCount : 12,

    //Represents the RCL level the creeps behave like.
    //Change once the preparations for a behavior shift are met.
    //ex. RCL5 is reached, but you haven't built links for harvesters to work off of.
    behaviorLevel : 1,
  }